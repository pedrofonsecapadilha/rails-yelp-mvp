<div class="container">
  <h1 class="mt-3 mb-3">RESTAURANT - <%= @restaurant.id %> </h1>

  <div class="card mb-3">
    <div class="card-body">
      <h5 class="card-title"><strong><%= @restaurant.name %></strong></h5>
      <h6 class="card-title"><%= @restaurant.category %></h6>
      <p class="card-text mb-0"><strong>Phone-number:</strong> <%= @restaurant.phone_number %></p>
      <p class="card-text mt-0"><strong>Address:</strong> <%= @restaurant.address %></p>
    </div>
  </div>


</div>

<%#
  <div class="card mb-3">
    <div class="card-body">
      <h5 class="card-title"><strong>Reviews</strong></h5>
      <% @restaurant.reviews.each do |review| %>
          <%# <p class="card-text mt-0">
            <%= review.content %>
            <%# <%= link_to "Remove", review_path(review), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
          <%# </p>
      <% end %>
    <%# </div>
  </div> %>

<%#
  <p><%= link_to 'New Review', new_restaurant_review_path(@restaurant), class: 'btn btn-success' %>

<%#
  <%= link_to "Edit this restaurant", edit_restaurant_path(@restaurant) %> |
  <%# <%= link_to "Back to restaurants", restaurants_path %>
  <%# <%= button_to "Destroy this restaurant", @restaurant, method: :delete %>
